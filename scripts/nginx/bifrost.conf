# Nginx configuration for Bifrost services
# To be deployed on Web-Server (10.0.0.75)

upstream fastapi_backend {
    server 10.0.0.80:8000;
}

upstream django_backend {
    server 10.0.0.80:8001;
}

server {
    listen 80;
    server_name bifrost.local;

    # Portal UI static files
    location / {
        root /opt/bifrost-portal-ui/build;
        try_files $uri $uri/ /index.html;
    }

    # FastAPI API endpoints
    location /api/ {
        proxy_pass http://fastapi_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Django admin
    location /admin/ {
        proxy_pass http://django_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket support (for real-time updates)
    location /ws/ {
        proxy_pass http://fastapi_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# HTTPS configuration (Phase 5 - SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name bifrost.local;
#     
#     ssl_certificate /etc/letsencrypt/live/bifrost.local/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bifrost.local/privkey.pem;
#     
#     # ... same location blocks as above
# }

